<%= form_for(@rspec_model) do |f| %>
  <% if @rspec_model.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rspec_model.errors.count, "error") %> prohibited this rspec_model from being saved:</h2>

      <ul>
      <% @rspec_model.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
